<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.software.application">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SpeechToText"
            android:label="@string/SpeechToText">
        </activity>
        <activity
            android:name=".Settings"
            android:label="@string/SettingsName">
        </activity>
        <activity
            android:name=".InterviewList_v1"
            android:label="@string/list_of_interview">
        </activity>
        <activity
            android:name=".SelectedActivity_v1"
            android:label="@string/list_of_interview">
        </activity>
        <activity
            android:name=".WatchInterview"
            android:label="@string/WatchInterview">
        </activity>

    </application>

</manifest>